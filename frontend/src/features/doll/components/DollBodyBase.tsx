import { MasterBody } from '@/features/doll/assets/MasterBody';

/** The illustrated Master Doll -- the raster figure every garment layers onto. */
export function DollBodyBase() {
  return <MasterBody />;
}
